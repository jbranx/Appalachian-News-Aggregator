name: Daily News Aggregator

on:
  schedule:
    - cron: '0 7 * * *'
  workflow_dispatch:

jobs:
  run-news-aggregator:
    runs-on: ubuntu-latest
    
    steps:
    - name: Check out repository
      uses: actions/checkout@v3
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
    
    - name: Debug - List files
      run: |
        echo "Current directory:"
        pwd
        echo "Files in current directory:"
        ls -la
        echo "Looking for news_aggregator.py:"
        find . -name "news_aggregator.py"
    
    - name: Run news aggregator
      env:
        ANTHROPIC_API_KEY: ${{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}}
        NEWS_API_KEY: ${{ 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 }}
        EMAIL_ADDRESS: ${{ jbranscome@gmail.com}}
        EMAIL_PASSWORD: ${{ dpyh hzsf iood qxmf }}
        RECIPIENT_EMAIL: ${{jbranscome@gmail.com }}
      run: python news_aggregator.py
